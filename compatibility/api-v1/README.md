# v1 declaration baseline

The `.d.ts` files in this directory are generated by `npm run api:update`.
They are reviewed compatibility evidence, not handwritten source. CI compares
all built declarations byte-for-byte with this baseline.
