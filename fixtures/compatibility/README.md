# Serialized compatibility fixtures

These documents are the v1 release-candidate compatibility baseline. Tests
must continue to load, validate, round-trip, and deterministically serialize
them. Additive formats require new fixtures; changing an existing fixture
requires an explicit compatibility review.
